# Project defaults; enforced organization and session policies still apply.
# Keep the user's active model and reasoning preference.
approval_policy = "on-request"
approvals_reviewer = "auto_review"
sandbox_mode = "workspace-write"
